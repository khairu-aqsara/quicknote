{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "QuickNote runs every privileged operation in Rust. The frontend needs only the core event channel and the asset protocol for local images.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
